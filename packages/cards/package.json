{
  "name": "@mc/cards",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Card ability scripts: the ability DSL (compiles to plain-data AbilityDefinitions), per-set ability modules, the assembled registry, and scenario/deck builders.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mc/content": "workspace:*",
    "@mc/engine": "workspace:*"
  }
}
