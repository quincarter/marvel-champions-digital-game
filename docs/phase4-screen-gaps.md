# Phase 4 follow-up: screens the design canvases draw that the client doesn't

Owner: `game-client-engineer`, with `game-rules-architect` for the engine queries in §2.

This is a subplan of PLAN.md Phase 4. It lists every screen in `Marvel Champions game screens/` that the client is missing or only partly implements, and breaks the work into independent workstreams (§3) that can be picked up one at a time. If you land or change a workstream, update this file in the same change, and tick its box in §3.

Inventory taken 2026-09-13 against `feature/phase7-8-9`.

## 0. Sources and ground rules

**Canvases** (all in `Marvel Champions game screens/`):
- `Screens - Desktop.dc.html`: sections 00 (flow map) to 14. Section numbers below (`D02`, `D13`…) refer to these.
- `Screens - Phone.dc.html`: `P01`–`P17`.
- `Screens - Tablet.dc.html`: `L01`, `L02`, `P01`, `P02`, `L05`–`L08`. Its portrait screens are also numbered `P01`/`P02`, so they are cited here as `T-P01`/`T-P02`.
- `Board - Long Table.dc.html`, `Board - Phone.dc.html`: the board.
- `Components.dc.html`: the design system.

**Ground rules, unchanged from Phase 4:**
- **Phaser is a view, never an authority.** Every rules fact on these screens (legality, damage, threat, deck legality) comes from `@mc/engine`. A screen that needs a fact the engine doesn't expose waits for an engine query (§2). It never computes the fact itself.
- **The mocks' card text is placeholder** (PLAN.md Phase 4, "Design source"). Names, stats and rules text come from `@mc/content`. See §5 for the mock content that is wrong for the Core Set.
- **Dashed = not yet real.** An action a screen shows before it exists is drawn unavailable with its reason, not omitted. Game Over already does this.
- **Hidden information stays hidden.** Previews and advice must use only what the deciding player may see (`view/visibility.ts`). A facedown boost card is a range, never a peeked value.
- **"Seat · AI" is "Seat · you"** (PLAN.md Phase 4, hero seats). Hero AI is Phase 8 backlog.
- New view models are plain TypeScript with Vitest tests, and scenes stay thin (PLAN.md Phase 4).
- **Match the canvases' look, not just their parts (added 2026-09-17).** Every screen workstream reads the rendered canvases for its screens (all three form factors) before building and finishes with side-by-side screenshots — [design-reference.md](design-reference.md) has the renders, a tile index and the procedure. The tokens and `Mc*` widgets already exist; what has been missing is each screen's composition (grounds, split, hierarchy, density).

## 1. Inventory

Status: ✅ built · ⚠️ partial · ❌ missing · ⛔ out of scope.

| Canvas | Screen | Status | Today | Workstream |
|---|---|---|---|---|
| D01, P01 | Title | ✅ | `scenes/title.ts`: the D01 menu. | W2 |
| D02, P02 | Scenario select | ✅ | `scenes/scenario-select.ts`. List rows, not a card grid; no record line yet. | W2 |
| D03, P03, T-P02 | Hero select ("Take your seats") | ✅ | `scenes/seats.ts`. | W2 |
| D04, P04 | Deck builder / Deck check | ⚠️ | `scenes/deck-builder.ts`: identity, aspect, type filter chips, name, text search, add/remove, live legality chip, cost curve, grouped deck list with overflow, Preconstructed/Clear. `scenes/deck-check.ts` (new, W1): Curve/Cards/Aspect tabs, deck list with quantities, Edit deck, Start game ▸ (drawn unavailable — no setup flow yet, W2). Neither is yet a step in the setup flow; deck advice not built (§4 unsettled). | W1 |
| D05, P12 | Table setup | ✅ | `scenes/table-setup.ts`. | W2 |
| D06, P13, L05 | Setup deal & mulligan | ❌ | The mulligan is a generic pending choice in `scenes/choice.ts` over the board. | W3 |
| D07, Board canvases, L01, T-P01 | Board | ✅ | Long table and phone tabs. Small chrome gaps. | W8 |
| D08, P14 | Inspect | ✅ | `scenes/inspect.ts`. No per-game history. | W8 |
| D09, L06 | Targeting | ⚠️ | Target mode dims and highlights. The prompt is one line in the action bar. | W5 |
| D10, P15 | Pending choice: defend | ✅ | `scenes/choice.ts`'s `declareDefender` branch over `defendPreview`/`stackEntries`. | W6 |
| D11, P09, L02 | Villain phase | ✅ | `scenes/villain-phase.ts`: step strip, "happening now" breakdown, queue, threat callout, inline interrupt, tablet team rail. | W7 |
| D12, P10, P11, P17, L08 | Game over | ✅ | Wide and tall layouts, stats, turning points, seats, MVP, both rematches. | W8 |
| D13, P16, L07 | Pause & Rules | ❌ | Nothing. The board has no menu button. | W4 |
| — (P16, L07, D01) | Settings | ❌ | `settings.ts` holds `reducedMotion` and `textResolution`; no screen shows them. | W4 |
| D14 | Decks & Collection | ✅ | `scenes/decks.ts`: two-pane list + stats (tabs on narrow), S8 filters, S4 record, duplicate/export/play, import, Check into Deck check. | W1, W9 |
| D01 | Campaign | ⛔ | Drawn locked in the mocks. Out of scope (PLAN.md Phase 4). | — |

`SCENES.setup` is Table setup; `SCENES.scenarioSelect` and `SCENES.seats` are the two steps before it.

## 2. Shared prerequisites

These are needed by more than one workstream. Each is small enough to land on its own, ahead of the screens that use it.

**Next, as the user asked (2026-09-16):** the shared prerequisites come before any workstream. Undo is tabled (PLAN.md Phase 8). They run one agent at a time, starting after the play-bug fixes currently running in the client are checked:
1. **Client batch (`game-client-engineer`): done, 2026-09-16.** S1, S2, S3, and S8's remaining quick-filter chips — see each entry below. All four landed as plain client view models with tests, with no engine/`@mc/cards`/`@mc/content` change.
2. **S4 (`game-client-engineer`): done, 2026-09-17.** The history view model from `GameStorage.list()`. The deck id on a custom-deck seat was designed with `multiplayer-netcode-engineer` first, since `SessionConfig` is also the save and network shape — see its own landed note under S4 below.
3. **S7 (`game-client-engineer`): partly done, 2026-09-17.** The replay-cursor model and its tests landed. The read-only board over it did not — see S7's own entry below for exactly what's left, for W4 and W8 to pick up.
4. **S6 (`card-data-pipeline`): done, 2026-09-17.** Keyword definitions for the glossary, paraphrased with RRG 1.8 page cites, as §4 recommends — see S6's own entry below for what landed, what's unverified, and one RRG-vs-ruling conflict found and flagged rather than resolved.
5. **S5 (`game-rules-architect`): designed and implemented, 2026-09-17** — see the "Implemented" note at the end of S5. The design is written under S5 below. No code was written: `packages/engine/src/resolve/*` (`apply-effect.ts`, `effects-frame.ts`, `enemy-activation.ts`, `frames.ts`), `spec.ts` and `packages/cards/src/dsl/*` are being edited by another session, and steps 3–5 of the plan land in exactly those files. Steps 1, 2 and 6 don't touch them and can start as soon as someone picks this up; the rest sequences after the concurrent engine work lands and `pnpm test`/`pnpm typecheck` are a trustworthy baseline again.

- **S1. Deck stats view model** (`view/deck-stats.ts`, client). Resource/cost curve, average cost, counts by card type and by aspect/hero/basic, all from `@mc/content` card data. It's descriptive, not a rules query, so it belongs in the client. Used by W1 and W9.
  - **Landed (`game-client-engineer`, 2026-09-16).** `deckStatsOf(deck, pool)` in `view/deck-stats.ts`, tested against every Core precon, the Doctor Strange wave 1 precon and a custom deck (`view/deck-stats.test.ts`). **X costs:** every printed player-card cost is typed `number` in the schema (never `PrintedStat`'s `"X" | null`), and no card in the pool has a variable printed cost, so the curve buckets by the number as-is; a future variable-cost card needs a schema change first, which this module doesn't attempt to anticipate. **No-cost cards:** `ResourceCard` has no `cost` field at all, so resource cards are excluded from `costCurve`/`averageCost` and counted only by type/aspect. **Identity separate decks** (Doctor Strange's Invocation cards) need no filtering: `Deck.cards` contractually excludes them already, checked against his real precon rather than trusted from the doc comment alone.
- **S2. Setup draft state.** The choices that span several setup screens: scenario, difficulty, modular sets, seats (deck id per seat), first player, seed. Today they're private fields on `TitleScene`. Move them into one plain object that each setup scene reads and writes, and that serializes to the existing `SessionConfig`. Used by W2 and W3.
  - `SessionConfig` already carries `modularSetIds` and `firstPlayerIndex` (`engine/host.ts:33-37`, passed through in `engine/session-core.ts:111-112`), so this is UI plumbing, not engine work.
  - **Landed (`game-client-engineer`, 2026-09-16).** `view/setup-draft.ts`'s `SetupDraft` holds scenario, difficulty, the per-villain version override, modular sets, seats, first player, seed and both roster filters, with a pure update API (`setScenario`, `setDifficulty`, `toggleVillainVersion`, `addSeat`/`removeSeat`, `pruneSeats`, the filter setters, `setSeed`/`rerollSeed`) and `toSessionConfig` to serialize it. The seed field's own mid-typing text stays a scene-local buffer, not part of the draft (`setup-draft.ts`'s own doc comment says why). `setup-draft.test.ts`'s round-trip test builds `SessionConfig` from a fresh draft and checks it matches, field for field, what `TitleScene#start` built by hand before this landed. `scenes/title.ts` now reads and writes one `#draft: SetupDraft` field instead of six separate ones; Title is still the only consumer.
- **S3. Encounter deck preview** (client, calling `@mc/cards`). The deck a table setup would build, by set and by card type, plus the nemesis sets held back and the obligations shuffled in. `coreScenario()` already builds the encounter deck from the scenario, the modular sets, Standard (+ Expert) and each hero's identity sets (`packages/cards/src/core/setup.ts`), so a preview should call the same builder rather than re-deriving the list. Used by W2.
  - **Landed (`game-client-engineer`, 2026-09-16).** `view/encounter-preview.ts`'s `encounterDeckPreviewOf(config, pool, encounterSets)` takes the `GameSetupConfig` a caller already built with `coreScenario`/`wave1Scenario` — no `@mc/cards` import needed inside the module itself — and reports each villain's deck by set and by card type (one deck for a single-villain scenario, one per villain for Breakout). No engine or `@mc/cards` change was needed: `coreScenario`/`wave1Scenario` are already pure, side-effect-free data assembly (no RNG, no `GameState`), so they can be called directly for a preview. The one thing they don't cover — each hero's obligation card and nemesis set (RRG 1.8 Appendix II, shuffled in during `createGame`'s own setup, a real `GameState` build) — is read straight off `HeroIdentityCard.obligationCardId`/`nemesisEncounterSetId`, the same two printed fields W2's own hero detail panel reads. Tested against Rhino (Core), Risky Business (single-villain wave 1) and Breakout (four villains, obligations/nemesis correctly off per The Wrecking Crew insert) — `view/encounter-preview.test.ts`.
- **S4. Results history.** Win/loss record per scenario, best clear, and per-deck record and last played.
  - Mostly derivable already: `GameStorage.list()` returns every recorded game with `status` `won`/`lost`/`abandoned`/`incompatible`, its `config` and its `round`.
  - **Gap:** a custom-deck seat is saved as `{ identityCardId, deck, aspects }` (`view/deck-seat.ts`), with no deck id, so a per-deck record can't be attributed. A precon seat is attributable by `starterDeckId`. Adding a deck id to the seat config needs `multiplayer-netcode-engineer`'s eye, since `SessionConfig` is the save and future network shape.
  - Used by W2 (scenario record) and W9 (deck record).
  - **Deck id landed (`multiplayer-netcode-engineer`, 2026-09-17).** The custom-deck case of `CorePlayer` (`packages/cards/src/core/setup.ts`) gained an optional `deckId?: string`; `corePlayerFromDeck` (`view/deck-seat.ts`) sets it to `deck.id` (this build's local deck-storage id) for every custom-deck seat. `SessionConfig.players` (`engine/host.ts`) carries `CorePlayer` unchanged, so the field rides through the existing save shape with no new save field and no schema bump (`engine/game-storage.ts`'s `SAVE_SCHEMA`) — S4's future view model reads it straight off `SaveMeta.config.players[i]` in `GameStorage.list()`, the same object precon attribution already reads `starterDeckId` off.
    - **Where it doesn't go.** Precon seats (`{ starterDeckId }`) don't get a `deckId` — `starterDeckId` already is that seat's stable attribution key, so a second id would only duplicate it; S4's model attributes a precon seat by `starterDeckId` and a custom seat by `deckId`, not by one uniform field. `@mc/engine`'s own `PlayerSetup` (`packages/engine/src/setup.ts`) has no `deckId` at all: `coreScenario` and `wave1Scenario`'s `seatsOf` (`packages/cards/src/core/setup.ts`, `packages/cards/src/wave1/setup.ts`) map a seat's fields into `PlayerSetup` by name rather than spreading it, so the field can't reach setup, RNG or a replay even by accident. `packages/cards/src/core/setup.test.ts` and `engine/session-core.test.ts` prove `coreScenario`'s output and the engine's resulting state/events are byte-identical with and without it on an otherwise-identical seat.
    - **Backward compatibility.** The field is optional and nothing reads it as a rules input, so a save written before this change (no `deckId` on its custom seats at all) still loads, lists and replays exactly as before — it's simply unattributed, the same as any seat S4 can't resolve today. `engine/session-core.test.ts` adds this as an explicit old-shape case (a custom-deck seat with no `deckId` field at all, not merely `undefined`) alongside the existing pre-wave-1-pool compatibility tests in that file, and `engine/game-storage.test.ts` proves the field itself round-trips verbatim through `create`/`load`/`list`.
    - **Deck identity over time.** The id names a deck-storage row (`Deck.id`, `@mc/content`), not a snapshot of that deck's contents that day. Editing a deck after playing it keeps the same id, so its later games merge under one id rather than splitting by revision; deleting a deck makes the id resolve to nothing, so S4 should treat a non-resolving `deckId` as "unattributed" rather than an error. There's no content/revision hash yet — deferred with W9's "Recently changed" (deck revision history), not built here.
    - **Phase 5 (networked games).** `deckId` only resolves against *this client's own* local deck storage; a remote seat's `deckId` is meaningless (and, if `Deck.id` generation ever becomes non-random, potentially collidable) against another client's storage. It carries no rules meaning — nothing downstream of `@mc/cards` reads it — so a hostile or stale value from a peer is not a trust problem, only a display one: each client should resolve *only its own* seat's `deckId` against its own storage for attribution, and never resolve another seat's `deckId` against its own storage (id namespaces aren't shared across clients) or treat a peer-supplied `deckId` as authoritative for anything. If Phase 5 ends up broadcasting `SessionConfig` to other clients verbatim, that's fine to leave as is — it's an opaque per-client string, not sensitive data — but a future revision could namespace it (e.g. pair it with a peer/session id) or strip it entirely from what's sent to other clients if it turns out to invite confusion.
    - Files: `packages/cards/src/core/setup.ts` (`CorePlayer`), `packages/client/src/view/deck-seat.ts` (`corePlayerFromDeck`), tests in `packages/cards/src/core/setup.test.ts`, `packages/client/src/view/deck-seat.test.ts`, `packages/client/src/engine/session-core.test.ts`, `packages/client/src/engine/game-storage.test.ts`. No change to `@mc/engine`, `packages/client/src/view/setup-draft.ts`, or `SAVE_SCHEMA`.
  - **Landed (`game-client-engineer`, 2026-09-17).** `view/results-history.ts`'s `resultsHistoryOf(saves)` takes whatever `GameStorage.list()` returned and answers both consumers: `ScenarioRecord` per scenario id (wins, losses, games played and best-clear rounds, kept both `byDifficulty` and `combined`, since an Expert clear and a Standard clear aren't the same achievement — `SessionConfig.difficulty` also carries Breakout's "extreme") for W2, and `DeckRecord` per `DeckKey` (wins, losses, games played, last played) for W9. **Which statuses count, decided from what `SaveStatus` actually is (`engine/game-storage.ts`):** `won`/`lost` are results; `abandoned` is played (a real session, just unresolved) but neither a win nor a loss; `incompatible` and `active` count as nothing at all — `incompatible` is always a save that was still `active` when a later build broke its replay (a finished `won`/`lost` game is never resumed, so its result is never overwritten), so it never had a trustworthy result to record, and `active` is simply not over yet. **Deck attribution** uses a namespaced `DeckKey` (`{ kind: "starter", starterDeckId }` vs `{ kind: "custom", deckId }`, per the deck-id note above) so a precon id and a custom deck-storage id can never collide even sharing a raw string; a game with N seats credits each *distinct* key once; an old-shape custom seat with no `deckId` at all is unattributed — it still counts fully in its scenario's record, but the model exposes `unattributedGameCount`/`unattributedSeatCount` (scoped to played games) so a screen can say so rather than show a silently low deck count. A `deckId` that no longer resolves to a stored deck is left for the caller to detect against live deck storage; this module only keys by the id. Tested (`view/results-history.test.ts`, 13 tests) against: empty input; every `SaveStatus`; best clear picking the lowest round among wins only, per difficulty and combined, never a loss's round; multi-seat same-deck crediting once; starter/custom key collision-avoidance with the same raw string; an old-shape unattributed seat (counted in the scenario, absent from any deck, surfaced in the unattributed counts); last played as the max `updatedAt`; and a full shuffle of the input producing byte-identical output (the module sorts its own `scenarios`/`decks` output by key rather than relying on `Map` insertion order). No engine/`@mc/cards`/`@mc/content` change; `pnpm test`/`pnpm typecheck` stayed green (content 246, engine 491, cards 434, client 491 → 504).
- **S5. Outcome preview queries** (`game-rules-architect`, `@mc/engine`). Needed for W5 and W6.
  - For a pending target choice: each legal target's result ("14 HP → 9 HP", "defeated", damage lost to no Overkill, a Guard cleared). Probe the effect through the pure engine, the way `legalActions` probes commands, so no rule is restated.
  - For a defend choice: each option's damage as a range over the unrevealed boost, using only public information.
  - "Why not the others?": group each present-but-illegal target by the engine's reason. `legalActions` already returns blocked targets with reasons, so check whether that covers pending choices before adding anything.
  - **Design (`game-rules-architect`, 2026-09-17).** Nothing written under `packages/` — see the "Next" note above for why. RRG cites are page numbers in `mc_rulesreference_v18_compressed.pdf` (read out of the repo file with a local PyMuPDF script in the scratchpad; this sandbox's `Read` tool still can't render the PDF, same as S6). Printed page numbers and PDF page indexes match, so a cite is unambiguous.

    - **How `legalActions` probes, and what a probe costs.** `probe()` (`packages/engine/src/legal.ts:108-111`) is the whole mechanism: it calls `applyCommand(state, command, deps)` and keeps only `ok` / `error.code` / `error.message`, throwing the resulting state away. `evaluate` (`legal.ts:220-271`) runs one probe per (target × wallet) variant and turns the failures into `blockedTargets`. It works because `applyCommand` is pure (`engine.ts:22-43`) and `Ctx.state` is *replaced*, never mutated (`ctx.ts:15-21`, `updateInstance`/`moveCard`/`setFrame` all rebuild), so a probe cannot touch the caller's state — RNG included, since `RngState` is a field of `GameState` (`state.ts:298`). Cost: `legal.ts:588-598` records 5–8 ms for a whole `legalActions` (which is dozens to hundreds of probes) and 0.3 ms for `paymentFor`, so a single probe is well under 1 ms. A 10-target preview is a handful of milliseconds, and the client already runs the engine off the main thread in `EngineHost`'s worker. No caching, no memoisation: a preview is cheap and must always be computed against the state as it is right now.

    - **S5.1 One query, not two: `preview(state, command, deps?)`.** W5 needs previews for two different things — a basic attack's target list (not a pending choice: `LegalAction.targets`, `legal.ts:72`, and the client dispatches a `basicAttack` command) and a pending `chooseTarget`'s options (`PendingChoice.options`, answered with `resolveChoice`). Both are "what does this command do?", so there is one query over `Command`, the same shape `tryPayment` already uses (`legal.ts:651-665`). New file `packages/engine/src/preview.ts`:
      ```ts
      export type PreviewStop =
        | { readonly kind: "complete" }
        | { readonly kind: "choice"; readonly playerId: PlayerId; readonly prompt: ChoicePrompt; readonly soleDecider: boolean }
        | { readonly kind: "hiddenInformation"; readonly after: GameEventType }
        | { readonly kind: "rejected"; readonly reason: EngineErrorCode; readonly message: string };

      export interface CounterSnapshot {
        readonly inPlay: boolean;
        readonly damage: number;
        readonly remainingHitPoints: number | null;   // null for a non-character
        readonly maxHitPoints: number | null;
        readonly threat: number | null;               // null for a non-scheme
        readonly threatLimit: number | null;          // main scheme: the value that completes it
        readonly exhausted: boolean;
        readonly statuses: StatusCounts;
      }
      export interface PreviewCounter { readonly instanceId: InstanceId; readonly before: CounterSnapshot; readonly after: CounterSnapshot }

      export interface OutcomePreview {
        readonly stop: PreviewStop;
        readonly events: readonly GameEvent[];        // the probe's own events, in order, truncated (see S5.3)
        readonly counters: readonly PreviewCounter[];
        readonly outcome: GameOutcome | null;
      }
      export function preview(state: GameState, command: Command, deps?: EngineDeps): OutcomePreview;
      ```
      The client builds the candidate command exactly as it already does for dispatch (`legalActions`' `example`/`targets`, or `tryPayment`'s accepted command), calls `preview` once per candidate, and words the result itself.

    - **S5.2 The diff is derived from the events, with totals read off the two states — not from a structural state diff.** Both were considered; events win for three reasons. (1) A numeric diff of `instances` says `damage: 9 → 14` and cannot say *why*, but the engine already emits the distinctions the design asks for: `damagePrevented{reason:"tough"|"cancelled"|"effect"|"cannotTakeDamage"}` (`events.ts:25`), `overkillSpilled` (`:98`), `threatRemovalBlocked` (`:118`), `uniqueEntryBlocked{disposition}` (`:109`), `leavePlayBlocked` (`:120`), `activationSkipped{reason}` (`:37`), `statusRemoved{reason}` (`:32`), `hitPointsSet` as distinct from `damageHealed` (`:31`, `:29`). "Toughness absorbed it" and "the excess was lost for want of overkill" are literally an event and the *absence* of one. (2) Events carry order; a state diff doesn't, and order is what the player is being asked about. (3) A generic `GameState` differ would need maintaining forever as the state shape grows, and would happily surface deck order — a hidden-information hazard by default. Returning the events themselves rather than a parallel `PreviewChange` union also means `view/log-lines.ts` already words every one of them (`log-lines.ts:205-213` is exactly "ATK 3 + 2 boost − 1 defense"), so W5's panel is mostly `logLine` in the future tense, and a new event kind flows through without an engine change. That is the *structured, not strings* part: `OutcomePreview` contains no prose.
      What events can't give is a total, because adding damage to a max-HP is a rule. So `counters` carries before/after for every instance the events named, each side read through the engine's own existing selectors (`remainingHitPoints`, `characterProfile`, `mainSchemeValue`, `query.ts`) — the same functions the live board reads — against the pre-state and the probed state. "14 HP → 9 HP" and "defeated" come from `counters`; "Toughness absorbed it", "Guard cleared", "no overkill, so the rest was lost" come from `events`. Nothing is recomputed and no rule is restated.

    - **S5.3 Where a preview stops.** Three stops, in the order they're checked:
      - **A nested choice.** Free: `applyCommand` returns as soon as `runFlow` parks a `PendingChoice` (`engine.ts:31-38`), so the probe halts there by construction. Report `stop: {kind:"choice", prompt, playerId, soleDecider}` so the panel can say "…and then you'll choose a target". **The preview never auto-answers a nested choice.** Auto-answering would be the engine deciding, and would be wrong exactly when the answer matters. If W5 later wants "and then, whichever you pick, …", that is the client calling `preview` again on each answer — a tree it builds, not one the engine invents.
      - **An interrupt/response window.** Falls out of the same rule and lands on the right side of it. A *forced* interrupt or response resolves inside the probe with no choice parked, so a Forced Interrupt that changes the attack *is* in the preview — correct, since it is mandatory and public (RRG 1.8 p. 5 "Ability — Simultaneous Timing Priority"; attack step 6, p. 9). An *optional* window parks `chooseTriggers` (`choices.ts:43`, `resolve/window.ts`) and stops the preview, which is also correct: whether another player uses a card is not something the engine may predict. One consequence to word in the UI: a preview is what happens *if nobody responds*.
      - **Hidden information.** The probe walks the real state, so left alone it would compute the true post-flip damage and the true drawn card. The guard is a truncation rule stated over zones, not over a hand-maintained list of "risky" event types, so it can't rot:
        1. classify every instance against the **pre-probe** state — *zone-hidden* if it is in a player deck, an encounter deck or a separate deck that the deciding player is not already being shown (the same "a deck is closed except for what an open decision offers" rule `view/visibility.ts` states for the client; the engine should own the predicate and the client's `faceVisible` should delegate to it rather than the two forking); *face-hidden* if it is out of a deck but `faceup === false` (a facedown boost card, a dealt encounter card, a facedown Drone, a tucked card, a set-aside nemesis set);
        2. truncate `events` at the first event that (a) names a zone-hidden instance, (b) turns a face-hidden instance faceup, or (c) follows a change in `state.rng`;
        3. report `stop: {kind:"hiddenInformation", after}`.
        So the honest answer to "the outcome depends on hidden info" is **a certain prefix plus an explicit unknown**, never a peeked answer. This is also what keeps `preview(endTurn)` from narrating a whole villain phase: it stops at the first dealt encounter card.
      - **Rejected.** If `applyCommand` refuses, `stop: {kind:"rejected", reason, message}` carries the engine's own code and message, the same pair `legal.ts:110` produces. That is what the target list dims a target with when it was reached some other way.
      - **The invariant to test, not merely argue:** an effect could *read* a hidden card without emitting an event that names it ("if the top card of your deck is a Weapon, deal 2"). Rules 1–3 wouldn't catch that. So the binding property is **permutation invariance**: shuffling the cards within any hidden zone must not change a single byte of the returned `OutcomePreview` (RRG 1.8 p. 27 "Look, Looked-At" — only a player an ability instructs to look may see those cards; p. 15 "Deck" and p. 17 "Encounter Deck" — deck order is fixed but not readable). It is cheap to test over a corpus and it is the test that finds the one card that breaks the zone rule; when one does, that effect kind gets marked hidden-dependent rather than the rule being loosened.

    - **S5.4 Determinism and replay.** A preview is a query, never a command. `GameLog` only grows through `sessionApply` (`engine.ts:189-201`) and `appendCommand`; `preview` calls `applyCommand` directly and discards the state, exactly as `probe` does, so a preview cannot enter the log, cannot advance `state.rng`, cannot advance `nextChoiceSeq`/`nextFrameSeq`, and is invisible to `replay()` and to `auditVillainPhases`. Two tests hold that: the passed-in state deep-equals a clone taken before the call, and a session that runs previews between commands produces a byte-identical `GameLog` to one that doesn't.

    - **S5.5 Defend preview: a range, computed ahead of the hidden cards.** `preview` is useless for the defend choice — it would truncate at the first `boostCardFlipped` — so the range is its own query, and it is the **one** place the engine does arithmetic in front of hidden cards. That is only allowable because the answer is permutation-invariant over them. New file `packages/engine/src/defend-preview.ts`:
      ```ts
      export interface BoostBound {
        readonly facedownCount: number;          // exact: see below
        readonly min: number; readonly max: number;
        readonly poolSize: number;               // how many cards the bound was taken over
        readonly mayTriggerBoostAbility: boolean;
        readonly poolWithBoostAbility: number;
        readonly scope: "unseen" | "wholeSet";
      }
      export interface DefendBand {
        readonly boostFrom: number; readonly boostTo: number;
        readonly damageDealt: number;            // after DEF
        readonly damageTaken: number;            // after tough
        readonly toughSpent: boolean;
        readonly defeated: boolean;
        readonly overkillToInstanceId: InstanceId | null; readonly overkillAmount: number;
        readonly retaliateToAttacker: number;
      }
      export interface DefendOptionPreview {
        readonly optionId: string;               // matches PendingChoice.options
        readonly defenderInstanceId: InstanceId | null;   // null = "decline"
        readonly basicDefense: boolean;
        readonly targetInstanceId: InstanceId;   // who actually takes the damage
        readonly exhausts: readonly InstanceId[];
        readonly baseAtk: number; readonly defenseReduction: number;
        readonly boost: BoostBound;
        readonly bands: readonly DefendBand[];
      }
      export function defendPreview(state: GameState, deps?: EngineDeps, opts?: { readonly scope?: "unseen" | "wholeSet" }): readonly DefendOptionPreview[] | null;
      ```
      Null unless the open `PendingChoice.prompt.kind === "declareDefender"`; the attack is read off the frame `PendingChoice.frameId` names (`choices.ts:124`; the choice is requested with `frameId: frame.frameId`, `resolve/enemy-activation.ts:280`).
      - **What "public" means here, precisely.** The **count** of facedown boost cards is exact and public, not a guess: the procedure gives them at stage `giveBoost` and only then moves to `declareDefender` (`enemy-activation.ts:213-219, 246-282`), matching RRG 1.8 p. 9 steps 1–2, so at the moment the choice is open every boost card for this attack is already on the enemy and `mustInstance(state, enemyId).boostCards.length` is the final number — additional ones from `extraBoost` and any dealt outside an activation (p. 11) included. The **icons on each card** are not public until the card is flipped in step 3 (p. 9, p. 11). The **composition of the unseen pool** is public: the players assembled the encounter decks themselves at setup, and "Each discard pile is open information, and may be looked at by any player at any time" (p. 16 "Discard Pile"), so *which* cards remain unseen is legitimately derivable; only their order is secret (p. 15 "Deck", p. 17 "Encounter Deck").
      - **The bound.** Pool for a given facedown card = every instance still in its home encounter deck (`CardInstance.home` / `discardZoneFor`, so multi-villain decks don't get mixed) **plus** every still-unrevealed facedown card that came out of that deck (the boost cards themselves, undealt-but-facedown encounter cards) — they are all equally unknown, and the k boost cards were drawn from exactly that multiset. If that pool is smaller than k, add the deck's discard pile, which is what a reshuffle would put back (p. 17). Then `max` = the sum of the k largest `boostIconsFor(state, deps, candidate)` over the pool and `min` = the sum of the k smallest, taken without replacement because each boost card is a distinct card. Using `boostIconsFor` (`modifiers.ts:107-123`) rather than the printed `boostIcons` is deliberate: a card in play can modify boost icons, and the preview must not restate that rule. The printed range is 0–3 (`content/src/schema/validation.ts:184-187`), so `3 × k` is available as a trivially safe fallback and `scope: "wholeSet"` (bound over every card in the assembled decks, seen or not) as a looser but unimpeachable one — `"unseen"` is the default. **Flagged, not silently decided:** the RRG never says in so many words that the encounter deck's *contents* are open information. It follows from how the deck is built plus the open discard pile, and every real player counts this way, but it is the one rules-adjacent judgement in S5; `scope` exists so the call can be changed in one place if the user wants the weaker claim.
      - **Boost abilities are never folded into a number.** A star icon marks a mandatory "Boost" ability (p. 11; p. 40 "Star Icon") that can do anything, and p. 11 is explicit that a star is *not* a boost icon and that damage dealt by a boost ability is not damage dealt by the activation — so a numeric ceiling that tried to include it would be dishonest twice over. The preview reports `mayTriggerBoostAbility` plus `poolWithBoostAbility` / `poolSize` (permutation-invariant counts; no card is named, and no probability is computed — that would be advice, which §4 hasn't settled) and the client words it "one of these may also carry a Boost ability". `bands` covers icons only.
      - **Bands, not a bare min/max.** The outcome can *change kind* across the range, so the query walks every integer boost total in `[min, max]` (at most `3k + 1` values) and collapses equal outcomes into bands. That is how "5–7 damage, and at 6 or more Spider-Man is defeated" gets said without the client doing arithmetic.
      - **The arithmetic itself is not restated.** `damage = max(0, atk + boostIcons − reduction)` lives inline in `enemy-activation.ts:298-312`, together with the two rules that surround it: a dashed ATK is an unmodifiable 0 so `vars.atkBonus` doesn't raise it (p. 15 "Dash (Value)"), and `reduction` is the defender's DEF **only** when `basicDefense && defenderProfile.kind === "identity"` (p. 9 step 4 and p. 15 — an ally's DEF never reduces; all the damage is dealt to the ally, and without overkill nothing carries over, p. 9 step 5). Extract that block as an exported pure `plannedAttackDamage(state, deps, frame, { boostIcons, defenderInstanceId, basicDefense })` and have `executeEnemyAttackFrame` call it with the real flipped total. One implementation, two callers, no drift.
      - **Status interactions, only where they change the public arithmetic.** *Tough* on whoever would take the damage: all damage prevented and one tough discarded (p. 44), except that a hero making a basic defense reduces by DEF first and **keeps** the tough if the result is 0 (p. 9 step 5, p. 44) — which is exactly a band boundary, and which the engine already gets right by returning early on `amount <= 0` (`resolve/event.ts:301`). *Overkill* fills `overkillTo`/`overkillAmount` on the bands where the defender is defeated (p. 31), and is absent otherwise — that absence is the "the rest was lost" the design asks for. *Retaliate X* on the defender is a fixed number, not a range: it triggers on "is attacked", not on damage (p. 38), so it is the same in every band — except that the character must still be in play after the attack (p. 38), so it drops to 0 in a band where the defender is defeated, and it is 0 outright against a `ranged` attacker. Read it through the same path the resolver uses (`keywordTotal(..., "retaliate", ...)` and the `ranged` check, `resolve/event.ts:406-411`). *Stunned*, *confused*, *stalwart* and *steady* change **nothing** in the defend preview and must not be mentioned there: a stunned enemy's attack is replaced by discarding the status before any of this, so no defend choice ever opens (p. 41 "Stun, Stunned"); confused cancels a scheme, not an attack (p. 13); stalwart (p. 40) and steady (p. 41) only govern whether those statuses stick, and steady means "stunned" must always be asked of the engine's own check rather than of `statuses.stunned > 0`. They belong to W7's "queued this phase" list (an activation that is going to be cancelled), not to W6.

    - **S5.6 "Why not the others?" — `legalActions` does not cover a pending choice, and can't.** When a choice is open, `legalActions` returns `{kind:"choice", choice}` and nothing else (`legal.ts:373`); `blockedTargets` is only ever built by `evaluate` for top-level commands (`legal.ts:236-242`). Worse, there is nothing to report even in principle: a `PendingChoice`'s `options` are built from the *already filtered* legal set — `selectTargets(state, effect.query, context)` for `chooseTarget` (`resolve/effects-frame.ts:601, 611-623`), `legalDefenders` for a defend (`enemy-activation.ts:158-174`) — so an illegal target is simply absent, with no record of why. The minimal addition, in two pieces:
      - `explainQuery(state, id, query, context): ExclusionCode | null` in `select.ts`. `matchesQuery` (`select.ts:182-242+`) is already a flat sequence of independent clauses, each an early `return false`; the refactor is mechanical — each clause gains a stable code (`wrongCategory`, `wrongController`, `notEngagedWithYou`, `missingTrait`, `hasExcludedTrait`, `wrongName`, `notAttached`, `wrongOwner`, `wrongAspect`, `exhausted`, `ready`, `noThreat`, …) and `matchesQuery` becomes `explainQuery(...) === null`, so there is one implementation and it cannot drift from the filter that actually ran.
      - `choiceExclusions(state, deps?): readonly { instanceId: InstanceId; reason: ExclusionCode }[]` in a new `packages/engine/src/why-not.ts`. It recovers the context the choice was built in **without any new field on `PendingChoice`**: `pendingChoice.frameId` → the `effects` frame → `frame.effects[frame.cursor]` is still the pending `chooseTarget` (`requestTargetChoice` requests the choice *without* advancing the cursor, `effects-frame.ts:62-63, 594-623`) and `contextOf(frame, deps)` (`effects-frame.ts:34-42`) rebuilds the `EffectContext` exactly. Then: candidates = the visible universe (`cardsInPlay`, or the named zone), minus the options already offered, each labelled with `explainQuery`. For `declareDefender` the same function reports the `legalDefenders` exclusions (`notInPlay`, `exhausted`, `alterEgoForm`, `notHeroOrAlly`, `defenderAlreadyDeclared`, `mustDefendWithAlly`) from the same three conditions that filter the list (`enemy-activation.ts:158-174, 248-254`).
      - It returns **codes, not sentences**: these are clause names, and the engine has no player-facing copy for them (unlike `EngineError.message`, which is written for exactly this). The client keeps one small code→wording table beside `view/highlights.ts`. Prompt kinds with no meaningful universe return `[]` — honestly empty, never a fabricated reason.

    - **S5.7 W6's stack question: `GameState` has the data, but not a readable shape.** `state.stack` is plain data and exported (`state.ts:278`), as are `StackFrame`, `StackView`, `viewStack` and `describeFrame` (`index.ts:139-140`). The open window is already addressable: `PendingChoice.frameId` names the frame the answer belongs to (`choices.ts:117-124`), so the "← here" mark is `frame.frameId === state.pendingChoice.frameId` — no new field needed. Two gaps: `describeFrame` (`stack.ts:247-268`) is a debug string ("interrupt window for enemyAttack"), and `StackView` carries nothing else to word a row from. So: one read-only selector, `stackEntries(state): readonly StackEntry[]` in `packages/engine/src/stack-view.ts`, with `{ frameId, depth (index; 0 is resolving now), kind, subjectInstanceId, eventKind, timing, stage, awaiting, openWindow, vars }`. `subjectInstanceId` is the private `frameCardId` (`ctx.ts:197-208`) exported and widened to cover `enemyAttack`/`enemyScheme` (the enemy). `vars` (already an exported type, `stack.ts:39`) is what answers W6's "any forced interrupt that changed it": `atkBonus`, `extraBoost`, `overkill`, `labeledDefense`, `undefended` all live in the `enemyAttack` event frame's vars (`enemy-activation.ts:107-110, 142, 217, 310, 330`). `describeFrame`/`viewStack` stay as they are for logs.

    - **S5.8 W7's breakdown question: the attack half exists, the scheme half doesn't.** `attackResolved` already carries every number separately — `{ enemyInstanceId, targetInstanceId, baseAtk, boostIcons, defenseReduction, damageDealt }` (`events.ts:51`, emitted at `enemy-activation.ts:313-321`), which `log-lines.ts:205-209` already renders as "ATK 3 + 2 boost − 1 defense". The flipped boost cards are `boostCardFlipped { enemyInstanceId, instanceId, boostIcons }` (`events.ts:46`), with `boostCancelled { scope: "icons" | "ability" }` (`:45`) for Attacrobatics/Target Acquired, and `defenderDeclared` / `defenseDeclined` / `defenderLeftPlay` (`:47-50`) say who defended, so "ally defended, so no DEF reduction" is readable without a new field. Missing: **the scheme side emits no breakdown at all.** `executeEnemySchemeFrame` computes `sch + boostIcons + threatBonus` and pushes only a `placeThreat` event (`enemy-activation.ts:382-398`), which becomes `threatPlaced { schemeInstanceId, amount, sourceInstanceId }` (`events.ts:33`) — a single total. Minimal addition: a `schemeResolved { enemyInstanceId, schemeInstanceId, baseSch, boostIcons, threatBonus, threatPlaced }` event mirroring `attackResolved`, emitted in the same place. Optional and not required by the canvases: `attackResolved` folds `vars.atkBonus` into `baseAtk`, so a screen that wants "ATK 2 (+1) + 3 boost" separately would need `printedAtk` or `atkBonus` on the event as well. **"Queued this phase" needs nothing new**: `state.step` in the `enemyActivations` case carries `currentPlayerId`, `remainingPlayerIds`, `villainActivated` and `activatedMinionIds` (`state.ts:227-235`), and `minionsEngagedWith` is exported (`index.ts:196`) — that is the engine's own order, exactly.

    - **S5.9 W4's Concede decision (§4): an engine outcome, not a client-only status.** Recommended, with the reasoning written down because the RRG has no concede rule at all and this is therefore a digital-implementation affordance, not a rule.
      - **Replayability decides it.** A log is `{ initialState, commands }` (`engine.ts:138-142`) and `replay()` is the only definition of what a saved game *was*. A client-only `abandoned` flag means the log replays to a live, unfinished game while the save claims the game is over: S7's `ReplayCursor.at(n)` could never reach the state Game Over is showing, "Watch the replay" (W8) would end mid-turn with no explanation, and `auditVillainPhases` would audit a game that has no ending. A `concede` command makes the log self-describing and terminal.
      - **Phase 5 decides the authority question the same way.** Command authority already lives in the engine — every `Command` names its issuer specifically so it can be checked in one place (`commands.ts:21-23`) and `resolveChoice` already rejects the wrong seat (`engine.ts:72-74`). A client-only flag gives a peer nothing to validate. Recommendation: **any seated, non-eliminated player may concede, and it ends the game for the whole table**, because a co-op table shares one outcome. There is no RRG basis for that (there is no RRG basis for conceding), so it is flagged rather than presented as a rule; if Phase 5 wants "every seat must confirm", that is a client/netcode wrapper that gathers confirmations and then issues the one command, *not* engine behaviour.
      - **Shape.** `{ type: "concede"; playerId }` on the `Command` union (`commands.ts:24-71`), and a **third result kind** on `GameOutcome` — `{ result: "conceded"; byPlayerId: PlayerId }` — rather than widening `loss` (`state.ts:243-249`). Widening `loss` would import a rules meaning the game doesn't have and would quietly turn a concession into a defeat in the win/loss record S4 just landed. A separate kind composes with what already exists at zero cost: `session-core.ts:117` maps it to the existing `"abandoned"` `SaveStatus`, which `view/results-history.ts` already counts as "played, neither a win nor a loss". One engine detail: `applyCommand` rejects everything but `resolveChoice` while a choice is pending (`engine.ts:31-33`), and conceding must work while the defend prompt is open, so `concede` needs an explicit exemption there with a comment saying why; the `state.outcome` guard above it (`engine.ts:28-30`) already makes a second concede an error, correctly.
      - **Watch out:** the four existing readers of `outcome.result` are ternaries, not exhaustive switches, so the type checker will **not** flag them and each silently reads "conceded" as a loss. They must be updated by hand: `view/log-lines.ts:244`, `view/game-over-model.ts:93`, `view/board-model.ts:510`, `engine/session-core.ts:117`.

    - **S5.10 Implementation plan, smallest first.** Steps 1, 2 and 6 touch none of the files the other session is editing (`resolve/apply-effect.ts`, `resolve/effects-frame.ts`, `resolve/enemy-activation.ts`, `resolve/frames.ts`, `spec.ts`, `cards/src/dsl/*`) and can start immediately. Steps 3, 4 and 5 must be sequenced **after** that work lands and `pnpm test`/`pnpm typecheck` are green again.
      1. **`stackEntries`** — new `packages/engine/src/stack-view.ts`, export `frameCardId` from `ctx.ts`, one line in `index.ts`. No collision. Unblocks W6's stack panel and W7 on its own.
      2. **`preview()`** — new `packages/engine/src/preview.ts` (S5.1–S5.4) plus the zone-visibility predicate it needs (engine-side, with `view/visibility.ts` then delegating to it instead of forking). Imports `applyCommand` and `query.ts` only. No collision. Unblocks W5's outcome column.
      3. **`schemeResolved`** — one variant in `events.ts`, one emit in `resolve/enemy-activation.ts:382-398`, one case in `view/log-lines.ts`. **Collides** (`enemy-activation.ts`).
      4. **`plannedAttackDamage` + `defendPreview`** — extract from `enemy-activation.ts:298-312`, new `packages/engine/src/defend-preview.ts` (S5.5). **Collides** (`enemy-activation.ts`). Unblocks W6's damage ranges.
      5. **`explainQuery` + `choiceExclusions`** — `select.ts` refactor plus new `packages/engine/src/why-not.ts` (S5.6). `select.ts` isn't being edited, but it is typed against `spec.ts`, which is; **sequence after** `spec.ts` settles. Unblocks W5's "Why not the others?".
      6. **Concede** — `commands.ts`, `engine.ts`, `state.ts`, the four client ternaries listed in S5.9. No collision, independent of 1–5, and it is what unblocks W4's Pause overlay.
    - **S5.11 Tests for `rules-qa-engineer`,** each tied to the page it is checking. Engine tests live beside the module (`src/**/*.test.ts`).
      - *Purity / RNG:* `preview` leaves the passed state deep-equal to a clone taken before the call, `state.rng` included (engine invariant, `engine.ts:22`, `ctx.ts:15-21`).
      - *Log:* a session that runs a preview before every command produces a byte-identical `GameLog` to one that doesn't (`engine.ts:189-201`).
      - *Stops at a choice:* previewing an attack that opens an **optional** response window reports `stop.kind === "choice"`; the same attack with only a **forced** response resolves inside the preview and the forced effect appears in `events` (RRG p. 5 timing priority; p. 9 attack step 6).
      - *Stops at hidden info:* `preview(endTurn)` in the last turn of a round stops at the first dealt/flipped encounter card (RRG p. 11 "Boost"; p. 27 "Look, Looked-At").
      - *Permutation invariance:* shuffle every hidden zone of a corpus of states, re-run every preview, expect byte-identical output — for both `preview` and `defendPreview` (RRG p. 15, p. 17, p. 27).
      - *Tough:* previewing 3 damage at a target with a tough status reports `damagePrevented{reason:"tough"}` and an unchanged HP counter (RRG p. 44).
      - *Overkill:* a lethal preview on a minion reports the defeat and **no** `overkillSpilled` without the keyword, and the spill amount with it (RRG p. 31).
      - *Guard:* previewing a basic attack on the villain with a guard minion engaged is `stop.kind === "rejected"` carrying the engine's own code, and is not once the minion is gone (RRG p. 21).
      - *No drift:* over a corpus of recorded games, the preview's `events` are a prefix of the events the real command actually produced (this is the test that catches the preview and the engine diverging at all).
      - *Boost count is exact:* at every `declareDefender` choice in a corpus, `boost.facedownCount` equals the number of boost cards that are then flipped (RRG p. 9 steps 1–3).
      - *Bound is honest:* at every one of those, the real post-flip damage lies inside the band range (RRG p. 11).
      - *Hero DEF vs ally:* a hero basic defense reduces by DEF; an ally defender's DEF never reduces, and without overkill nothing carries to the identity (RRG p. 9 steps 4–5, p. 15, p. 31).
      - *Tough hero band:* a tough hero making a basic defense keeps the tough in the band where the damage reduces to 0 and spends it in the bands where it doesn't (RRG p. 9 step 5, p. 44).
      - *Star icon:* an unseen pool containing a Boost-ability card sets `mayTriggerBoostAbility` and never raises `max` (RRG p. 11, p. 40 "Star Icon").
      - *Retaliate:* a defender with retaliate X reports X in every band where it survives, 0 where it is defeated, and 0 against a ranged attacker (RRG p. 38).
      - *Stack:* `stackEntries` marks exactly one `openWindow`, and it is the frame `pendingChoice.frameId` names (`choices.ts:124`).
      - *Scheme breakdown:* for every scheme activation in a corpus, `schemeResolved.baseSch + boostIcons + threatBonus` equals the `threatPlaced` amount that follows it (RRG p. 11; "Scheme (Enemy Activation)").
      - *Why not:* for a `chooseTarget` over "an enemy", `options ∪ choiceExclusions` equals the visible universe and each exclusion's code names the clause that actually rejected it (RRG p. 12 "Choose (Game Element)"; p. 21 Guard for the defended-villain case).
      - *Concede:* a conceded log replays to the same `conceded` outcome (`engine.ts:171`), concede is accepted while a `PendingChoice` is open, and rejected once `state.outcome` is set (`engine.ts:28-33`).
    - **Implemented (`game-rules-architect`, 2026-09-17).** All six steps of S5.10 landed (merged into `feature/phase4-screen-gaps`). `pnpm test`/`pnpm typecheck` green: engine 507→556, client +6. Signatures below are what W5/W6/W7 code against; they differ from S5.1–S5.9 only where noted.
      - **1. `stackEntries`** — `packages/engine/src/stack-view.ts`. `stackEntries(state): readonly StackEntry[]` with `{ frameId, depth, kind, subjectInstanceId, eventKind, timing, stage, awaiting, openWindow, vars }`, as designed; `describeFrame`/`viewStack` untouched. **Deviation:** `frameCardId` is exported from `ctx.ts` *unwidened* — widening it to cover `enemyAttack`/`enemyScheme` would have silently changed `perilOnStack`, whose rule is about the player resolving a *card* (RRG 1.8 "Peril", p. 32). The widening lives in `stack-view.ts`'s own `subjectOf`. Tests: `stack-view.test.ts` (5).
      - **2. `preview()`** — `packages/engine/src/preview.ts`, plus the engine-owned zone predicate in `packages/engine/src/visibility.ts` (`faceVisible`, `zoneHidden`, `faceHidden`, `offeredByOpenChoice`). `preview(state, command, deps?): OutcomePreview` as designed. **Deviations:** (a) the hidden-information stop is `{ kind: "hiddenInformation"; at: GameEventType }` — `at` is the kind of the event the preview stopped *at*, which is **not** in `events`. (b) Rule 2(c) ("truncate after a change in `state.rng`") has no per-event rng to locate, so when a command consumes RNG at all it is re-run on the same state under a different seed and the prefix ends at the first event that differs — one extra probe, only on commands that rolled. (c) Rule 2(b) ("turns a face-hidden card faceup") is tested against the *post*-state's `faceup`: damaging a facedown Drone does not truncate, defeating it does. (d) `outcome` is reported only when the preview was **not** truncated. **Client delegation done:** `packages/client/src/view/visibility.ts` is a one-line re-export of the engine predicate. One deliberate behaviour change: `separateDiscard` is now open like every other discard pile (RRG 1.8 "Discard Pile", p. 16). Tests: `preview.test.ts` (13) — purity incl. RNG, byte-identical `GameLog` with previews interleaved, events-are-a-prefix-of-the-real-command, Guard rejection with the engine's own code (p. 21), Tough (p. 44), overkill present/absent (p. 31), optional response stops vs forced resolves inside (p. 5, p. 9 step 6), `endTurn` stops before the draw touches a closed deck (pp. 15, 17, 27), permutation invariance over every hidden zone.
      - **3. `schemeResolved`** — one variant in `events.ts`, emitted in `executeEnemySchemeFrame` beside its `placeThreat`, plus the `view/log-lines.ts` case ("X schemed for N threat on Y (SCH a + b boost − c threat)"; the third term is omitted when nothing changed the threat). Tests: `scheme-breakdown.test.ts` (3).
      - **4. `plannedAttackDamage` + `defendPreview`** — `packages/engine/src/defend-preview.ts`. Shapes as designed plus `DefendOptionPreview.targetPlayerId` and a named `BoostScope`; `scope` defaults to `"unseen"`. **Deviation:** `plannedAttackDamage` and `overkillRecipient` both live in `defend-preview.ts` and are imported *back* by `resolve/enemy-activation.ts` and `resolve/event.ts` (`overkillRecipient` had to leave `resolve/` to avoid an import cycle; it gained an optional `defending` argument). `activationVarsOf(state, frameId)` is the single activation-vars lookup. Tests: `defend-preview.test.ts` (13) — exact facedown count vs cards actually flipped (p. 9 steps 1–3), real post-flip damage inside a band (p. 11), `wholeSet` never tighter, Boost ability reported and never raises `max` (p. 11, p. 40), shuffle-invariance, hero DEF vs ally (p. 9 steps 4–5, p. 15, p. 31), tough-hero band boundary (p. 44), retaliate/ranged (p. 38, p. 37), and the resolver's own `attackResolved` matching the band for all three answers.
      - **5. `explainQuery` + `choiceExclusions`** — `select.ts` refactor plus `packages/engine/src/why-not.ts`. `matchesQuery` is literally `explainQuery(...) === null`; each clause returns a stable `QueryExclusion`. `choiceExclusions(state, deps?)` handles `chooseTarget` (context rebuilt through `contextOf`, now exported from `resolve/effects-frame.ts`) and `declareDefender`; every other prompt returns `[]`. Codes, not sentences. Tests: `why-not.test.ts` (7).
      - **6. Concede** — DECIDED as S5.9 recommends. `{ type: "concede"; playerId }`; any seated non-eliminated player; accepted while a `PendingChoice` is open, rejected once `state.outcome` is set. **Deviation:** the outcome is `{ result: "conceded"; reason: "playerConceded"; byPlayerId }` — `reason` keeps `GameOutcome.reason` total for the eleven existing readers. All four client ternaries updated (`conceded.test.ts`, 5): board step label "Conceded", Game Over copy says conceded not defeated, neutral log line, `session-core` maps to the existing `"abandoned"` `SaveStatus`. `SAVE_SCHEMA` unchanged (events aren't stored; logs are commands). Engine tests: `concede.test.ts` (8), incl. a conceded log replaying to the same outcome.
      - **Still flagged, not resolved:** (i) the RRG never states that the encounter deck's *contents* are open information — `defendPreview`'s default `scope: "unseen"` rests on that derivation; `"wholeSet"` weakens the claim in one call. (ii) Conceding has no RRG basis; "any seat may concede for the table" is a product decision, and "every seat must confirm" would be a client/netcode wrapper, not engine behaviour. (iii) `preview`'s permutation invariance is a property test over a small corpus, not a proof.
- **S6. Player-facing keyword text.** `content/src/schema/keywords.ts` enumerates keywords but holds no definitions a player reads. The Pause screen's glossary needs short definitions, each tied to its RRG 1.8 page. Owner: `card-data-pipeline`. See §4 on wording.
  - **Landed (`card-data-pipeline`, 2026-09-17).** `schema/glossary.ts` (`@mc/content`, exported from the package index the same way `keywords.ts` is): 34 entries — all 31 `KNOWN_KEYWORD_NAMES` plus the three status cards (confused, stunned, tough), each an original one-to-two-sentence paraphrase (never FFG's own glossary wording) with a `sources` list citing a real page of `mc_rulesreference_v18_compressed.pdf` and, where a post-1.7 ruling clarifies it, that ruling's dated heading from `marvel-champions-rulings-post-rrg-1-7.md`. `glossaryEntry(id)` and `glossaryEntriesForKeywords(card.keywords)` are the lookup API W4 will call; the latter is tested against real Core cards (Luke Cage/Toughness, Hydra Mercenary/Guard, Vulture/Quickstrike, Black Panther's hero-face Retaliate). `glossary.test.ts` enforces one entry per enumerated id (both directions, via a `Record<KeywordName, …>` the type checker also rejects if incomplete), no duplicate ids, a length cap on every definition, and that every RRG page cite is within the PDF's real 71 pages.
    - **One keyword, "discount", is unverified:** RRG 1.8 has no glossary entry for it at all (confirmed by reading the whole PDF, not just guessing from its absence in the index), and `keywords.ts`'s own header traces it to Fear No Evil, a product not in this repo. Its entry is `{ kind: "insert-not-in-repo", product: "Fear No Evil" }` and `unverified: true` — the paraphrase says plainly that it isn't confirmed rules.
    - **One real RRG-vs-ruling conflict, flagged rather than picked:** RRG 1.8 p. 36 says quickstrike "resolves after any When Revealed abilities" on a minion being revealed; the February 28, 2026 - Ruling 4 answer (the Hellcat example) says the opposite — quickstrike resolves *first*. The `quickstrike` entry carries a `conflict` field spelling this out instead of silently choosing a side, and a test asserts it's the only entry with one.
    - **Left out, deliberately:** "exhausted", "ready", and "facedown boost card" are real RRG glossary entries (pp. 19, 36, 47) and are genuinely table-visible, but they're runtime instance state that only `@mc/engine`'s `state.ts` models (`StatusCounts`/`STATUS_NAMES` cover confused/stunned/tough only) — `@mc/content` has no enum for them today, and per this task's own scope and the `content` package's place at the bottom of the dependency direction, they don't belong here. Whoever builds the actual Pause screen glossary for W4 will need a second source for those three terms, from wherever the client already reads engine state, not from `@mc/content`.
    - **How the RRG pages were checked:** this sandbox's `Read` tool couldn't render the PDF (`pdftoppm`/poppler isn't installed, and installing it failed — no C compiler available to build it from source). Every page cited was instead read directly from the real repo file via a local PyMuPDF extraction script (not fetched from the web, not cited from memory), cross-checked against the RRG's own two-page glossary INDEX (pp. 2-3, which lists every term's page) before trusting a page number.
- **S8. Searchable, scrollable roster.** Scenario select and Take your seats both list things that will grow far past a screenful. The pool already has 3 Core and 3 wave 1 scenarios and 12 precons before any saved or imported decks, and every later pack adds more. So neither roster may be a fixed grid that assumes it fits.
  - **The list:** build on the virtualized list the deck screens use (`ui/virtual-list.ts`, PLAN.md Phase 7, "scroll lists"). Rows scroll by pixels, only visible rows are live objects, and the scroll state lives in a plain-TS model that survives a scene rebuild.
  - **The search:** a text field above the list, with a plain-TS filter model and tests.
    - Heroes match on hero name, alter-ego name, deck name, aspect and source (precon, imported, built). Scenarios match on villain name, scenario name, product or pack, and encounter set name.
    - Filtering is case- and accent-insensitive.
    - When nothing matches, the list says so and offers Clear.
    - The query persists while the player moves between setup steps (S2), and resets on a new setup.
  - **Quick filters** are chips beside the search:
    - heroes: aspect, source, and "playable now" (hides seats blocked by the engine's reasons in `view/seats.ts`, without dropping them from the data);
    - scenarios: product or cycle.
  - **Keyboard, pad and touch:**
    - Every row is a logical focus stop, and focusing a row scrolls it into view.
    - Page Up/Down and Home/End move through the list, and typing while the list has focus jumps to the search field.
    - A dimmed (blocked) row still takes focus, so its reason can be read.
  - **Layout:** the search and the list share one layout function with the rest of the screen, and a test checks the list's viewport never overlaps the controls above or below it. Checked at portrait phone (about 440×900), 800×600 and desktop sizes.
  - Used by W2's Scenario select and Take your seats, and by W9's deck list.
  - **Partly landed (`game-client-engineer`, 2026-09-15), on today's Title rather than W2's own screens.** `ui/virtual-list.ts` (`McVirtualList`) is the pixel-scrolling virtualized list, with `view/list-scroll.ts` for scroll state, `view/drag-gesture.ts` for tap-vs-drag and momentum, and `view/roster-filter.ts` for the search. The Decks list and the deck builder's pool use the same widget.
    - **Done:** the list, the search field with an empty-result message and Clear, every row a focus stop with scroll-into-view, Page Up/Down and Home/End, and layout tests.
    - **Seen working in the browser by the main session** at 800×600 and an emulated 375×812 phone: wheel, drag with a flick, and tapping a row; typing narrows both rosters; rows clip at the list edges.
  - **Quick-filter chips landed (`game-client-engineer`, 2026-09-16), on today's Title.** `RosterFilter` (`view/roster-filter.ts`) gained `aspect`/`source`/`playableOnly` (heroes) and `product` (scenarios), each `&&`ed onto the existing text match; `heroRosterMatches` takes the deck's current `blockedBy` as a separate argument (from `view/seats.ts`) so `playableOnly` can hide a blocked seat without `RosterFilter`/`Deck` needing to know why — a display filter only, nothing about `DeckOption`/`SeatOption` changes. `heroAspectsOf`/`scenarioProductsOf` derive the chip lists from what's actually in the pool today rather than a hardcoded set. `@mc/content` has no `Cycle`/`Pack` data wired into the app pool yet, so the scenario chip is "product" (`Scenario.packCode`) only — "or cycle" isn't buildable without that data.
    - `view/title-layout.ts` gained `scenarioChips`/`heroChips`, one fixed-height row each between a search field and its list, laid out by the same pure function as everything else on the screen; `title-layout.test.ts`'s no-overlap tests were extended to cover them.
    - `view/screen-focus.ts`'s `titleFocusOrder` gained optional `scenarioChipIds`/`heroChipIds`, inserted right after each search-field stop, so every chip is a keyboard/pad focus stop (`screen-focus.test.ts`).
    - `scenes/title.ts` draws both chip rows with the same equal-width-cell button row `#drawChoiceRow` already used for difficulty and Breakout's villain-version cells, so chips get McButton's touch target, hover/selected state and text auto-fit for free; each chip toggles its field on `SetupDraft`'s filter and rebuilds.
    - **Chip wrapping and short-viewport fit (2026-09-17).** Eight hero chips in one row truncated every label at phone width, and at 800×600 Start game fell below the fold. `view/chip-layout.ts`'s `wrapChipsToRows` packs chips into as many rows as their labels need (a conservative width estimate, no canvas), `TitleLayoutInput` takes the two row counts, and `titleLayout` now gives up whole roster rows (never below one) until Start fits. On a short viewport the room comes from the logo (`titleSize`, now on `TitleLayout` so the scene doesn't re-derive it), never from a section label's `LABEL_ROOM` or a 44px control. Seen in the browser at 800×600 and 375×812: every chip label reads in full, every section label is visible, Start is on screen. Both rosters are down to one visible row at those sizes — a cost of one page holding all of setup, which W2's split removes.
    - Not done: a friendly product name (the chip shows the raw pack code, e.g. "gob", the same shorthand the scenario row's own subtitle already shows) — deferred rather than inventing display names `@mc/content` doesn't carry yet.
- **S7. Log jump.** Replay a saved log up to command N into a read-only view, with the live game untouched. `replay()` exists (`packages/engine/src/engine.ts:171`). The work is a read-only board over a replayed state. It is also the basis for Game Over's "Watch the replay". Used by W4 and W8.
  - **Landed (`game-client-engineer`, 2026-09-17): the cursor model only. The read-only board is not wired — see "Not done" below.** `view/replay-cursor.ts`'s `ReplayCursor` yields the `GameState` (and the events that produced it) after any command count 0..length, with `at(n)`, `jumpTo`/`stepForward`/`stepBack`/`toStart`/`toEnd`, and a `moments` list of round/phase boundaries for a jump list. `ReplayCursor.fromStoredGame(config, storedInitialState, commands)` is built on the exact scenario-construction path `EngineSessionCore#resume` uses — a new export, `rebuildBaseline`, factored out of `resume` itself (`packages/client/src/engine/session-core.ts`) rather than a second scenario-building path, so a replayed state is exactly what resuming that save would produce (`resume` now calls it too; `session-core.test.ts`/`game-storage.test.ts` still pass unchanged). `ReplayCursor.fromLog` takes a live session's own `GameLog`/`SavedGame` (already carries its card pool) for the eventual "jump to a moment in the game you're playing" case. **Never touches the live session:** every constructor defensively `structuredClone`s what it's handed and holds no reference back into the caller's objects; `replay-cursor.test.ts`'s own test drives a live `EngineSessionCore` game, takes `core.save()` before and after moving a cursor built from it every way a scrubber would (`toEnd`/`toStart`/every `stepForward`/`stepBack`/an arbitrary `jumpTo`), and checks the live save is unchanged both by deep-equal and by reference (`savedAfter.initialState === savedBefore.initialState`) — and that the live session still dispatches normally afterward. **Determinism** is checked directly against a live game: `cursor.at(n).state` (minus `cardPool`, which only a live session's `StateWithoutPool` omits) equals what the live game's own state actually was after its `n`th command, for every `n` in a real playthrough — not only against a second, independently-replayed "ground truth" (a first attempt at that comparison forgot to pass `POOL_DEPS` to the ground truth's own `applyCommands` call, silently replaying with no abilities at all and diverging — a bug in the test, not the cursor; worth naming since it looked exactly like the engine determinism bug S7 was built to guard against). **Stepping-back cost:** `moments` already needs one full forward pass of the log (there's no other way to find every round/phase boundary), so the cursor keeps a state checkpoint every 20 commands as a free by-product of that pass, and `#resolve(n)` also caches every index it walks through on the way to `n`, not only `n` itself — so a jump costs at most ~20 engine calls fresh, and a one-command-at-a-time scrub through a whole stretch pays for that stretch once. Measured on this app's own realistic logs (a naive-greedy 4-player Ultron game, 100+ commands, and the Core e2e suite's smart-driven 214-command Ultron game as the upper reference): a full one-at-a-time walk of the whole log, backward then forward, stays within a small constant multiple of the log's length rather than the length² a naive "always re-replay from 0" cursor would cost (`replay-cursor.test.ts`'s own test asserts this on the applyCommand call count, not on wall-clock time). **Moments are keyed to where a command actually landed, not to events that happened and then were left behind inside one command:** the engine can run a whole villain phase and roll into the next round's player phase in one `endTurn` when nothing pauses it (`view/villain-walkthrough.ts` documents the same fact for its own event walk), and a command-granularity cursor has no index to jump to for a phase that opened and closed inside one command — so `moments` compares the *final* `state.round`/`state.step.phase` after each command against the previous command's, not the intermediate `stepChanged`/`roundStarted` events. A round moment is rarely lost this way (the round number the state settles at is right there even when the phase that opened it wasn't); a "Villain Phase" moment is lost whenever nothing pauses a solo/uninterrupted villain phase — real in a solo Rhino game with this build's own greedy-play test fixture, which is why `replay-cursor.test.ts`'s moments test uses a 4-player game (more defend decisions to pause on) rather than asserting a villain-phase moment always exists. Round-begins labels reuse `view/log-lines.ts`'s exact wording ("Round N begins"); the two phase labels ("Villain Phase", "Player Phase") are the RRG's own step-phase names, not new copy.
  - **Not done — the read-only board.** Wiring a replayed `GameState` into what the Board scene actually draws turned out to be a scene-sized job, not a seam, so it wasn't attempted: `appSession()` (`packages/client/src/session.ts`) is a single global singleton the Board reaches into directly, not just from `scenes/board.ts` and `scenes/board/controller.ts` but from `scenes/board/hand.ts`, `scenes/board/action-bar.ts` and `scenes/board/motion.ts` too (16 call sites across those five files) — there is today no seam to hand any of them a *different*, replayed state instead of the live one without either an injectable-state refactor across all five files or a fragile swap-and-restore of the global singleton, and the latter is exactly the kind of thing that could leak a stray command into the live game if it isn't restored perfectly on every exit path, which is the one thing this workstream cannot risk. That refactor, plus the "Replay · command N of M" bar and its step/exit controls, plus deciding how a pending choice in the replayed state should read when nothing can answer it, plus routing the replayed state through `view/visibility.ts` so a replay never shows a facedown card the deciding seat couldn't have seen at that moment, are what's left for W4 ("jump to a moment") and W8 ("watch the replay") to build, on top of the model landed here. No dev-only query-param entry point was added either, since there is nothing real to show through it yet — adding one for a half-built scene would be worse than leaving the gap documented.
  - **Landed anyway, as the safe minimal fallback the task allows for when the full wiring doesn't fit in a seam:** `scenes/board/controller.ts`'s `BoardController` takes an optional `{ readOnly: true }` at construction (or `setReadOnly(true)` later) and every method that would enter a target/payment/discard/controller-choice mode or dispatch a command becomes a no-op — checked independent of what the live session's own `legalActions` say, since (per the point above) a replay screen sitting over the live session could otherwise still see the *live* game's own legal actions and act on them by accident. `#dispatch`, the one choke point every mutating path already funnels through, is also guarded on its own, as a second line of defence. Not unit-tested: `BoardController` (like the rest of `scenes/board/`) has no existing test file, because it's a thin scene-layer class over `appSession()`'s global singleton and a real `Worker`-backed host, both outside what Vitest exercises anywhere in this package today (checked: no test imports `session.ts`) — introducing mocking infrastructure for a global singleton to cover one guard would be a bigger and less-precedented change than the guard itself. The default (`readOnly: false`) is unchanged behaviour, confirmed by the full client suite staying green.
  - Files: `packages/client/src/view/replay-cursor.ts`, `packages/client/src/view/replay-cursor.test.ts`, `packages/client/src/engine/session-core.ts` (`rebuildBaseline`, and `resume` refactored to call it), `packages/client/src/scenes/board/controller.ts` (`readOnly`/`setReadOnly`). No `@mc/engine`, `@mc/cards` or `@mc/content` change — `replay()`/`applyCommand`/`applyCommands` already stop at any prefix of a log.

## 3. Workstreams

Each workstream lists its canvases, what's already there, the work, what it depends on, and when it's done. They are ordered by suggested priority (§6), but only the listed dependencies are hard.

### W1. Deck analysis

Canvases: P04 (Deck check), D04 (Deck builder), D14 (right-hand stats panel).

Already there: `scenes/deck-builder.ts`, `view/deck-builder-model.ts` (`legalityOf`, `browsablePool`). `PoolFilter` already supports `type`, `trait` and `maxCost`, but the scene wires only the text search.

- [x] S1, the deck stats view model, with tests against the six Core precons (and a wave 1 precon, and a custom deck — see §2).
- [x] **Deck check screen** (P04). Resource curve with average, composition counts, tabs for Curve / Cards / Aspect, the deck list with quantities, "Edit deck", and "Start game ▸". Reached per seat from the setup flow (W2) and from the Decks screen.
  - **Landed (`game-client-engineer`, 2026-09-17).** New `scenes/deck-check.ts` and `view/deck-check-layout.ts` (a pure layout — header/tabs/content/footer — with no-overlap tests at 375×812, 440×900, 800×600 and desktop, `view/deck-check-layout.test.ts`). Tabs (Curve/Cards/Aspect) use `McTabs`; Curve shows `costCurveBars`' bar chart plus composition-by-type tiles; Aspect shows composition-by-aspect tiles (both via `view/chip-layout.ts`'s `wrapChipsToRows`, reused rather than re-derived); Cards is a `McVirtualList` over `deckListGroupsOf`'s grouped, quantified entries, each row also a real focus stop (`view/screen-focus.ts`'s new `deckCheckFocusOrder`, scoped to the active tab). Every number is `view/deck-stats.ts`'s own; this scene only draws it.
    - **The entry point is deliberately caller-agnostic**: `DeckCheckSceneData { deck; returnTo?; onStartGame? }`. `returnTo` (scene + optional data) defaults to the Decks screen, so a caller with only a deck can still open it; `onStartGame` is absent until a setup flow exists to hand the finished lineup to. **How W2 should open it**: `this.scene.start(SCENES.deckCheck, { deck, returnTo: { scene: SCENES.setup, data: {...} }, onStartGame: () => /* hand the seated lineup to the engine */ } satisfies DeckCheckSceneData)` — no change to this file needed. Until then, "Start game ▸" is a real, focusable control drawn unavailable with its reason ("Setup flow isn't built yet — coming with the title-menu rework (W2).") rather than omitted, per §0's "dashed = not yet real".
    - Reachable today from the Decks screen: every row (precon or saved) grew a "Check" button (`scenes/decks.ts`'s `rowGeometry`, `decksFocusOrder`), calling `#openDeckCheck` with `returnTo: { scene: SCENES.decks }`.
    - Not done: a step indicator ("Step N of M") — that belongs to the setup flow W2 hasn't built yet, and inventing one here would be decoration with nothing behind it.
- [x] **Builder stats panel** (D04). Cost curve, legality chip ("41 CARDS · LEGAL") and the deck list grouped by Hero / aspect / Basic with a "+ N more" overflow.
  - **Landed (`game-client-engineer`, 2026-09-17).** `scenes/deck-builder.ts`'s new `#drawStatsPanel`: a cost-curve bar chart (`costCurveBars`) and the deck list grouped Hero → aspect → Basic (`deckListGroupsOf`, new in `view/deck-stats.ts` alongside `costCurveBars` — both tested in `view/deck-stats.test.ts` against every Core precon), capped at 10 entry lines with one trailing "+ N more" rather than a second scrolling list, since the builder's pool already scrolls below it. The existing legality line stayed prose ("Legal — 41 cards." / the problem list) rather than being restyled into D04's own "41 CARDS · LEGAL" chip — same fact, not worth a second widget for the wording alone.
    - Not done: D04's three-column desktop layout (aspect/filter rail · pool · stats rail). The builder stays the one stacked column it already was at every width (unchanged architecture); the stats panel sits between the legality line and the new Preconstructed/Clear/Save row instead of its own right-hand rail. The workstream's own "done when" (curve, composition and grouped list at desktop *and* phone) holds regardless, since the single column already runs at every width.
- [x] **Builder filters.** Type filter chips (All, Ally, Event, Upgrade, Support, Resource) wired to `PoolFilter.type`, plus the aspect switcher D04 shows.
  - **Landed (`game-client-engineer`, 2026-09-17).** `TYPE_FILTERS` in `scenes/deck-builder.ts`: All/Ally/Event/Upgrade/Support/Resource, each toggling `PoolFilter.type`, wrapped to the column width with `view/chip-layout.ts`'s `wrapChipsToRows` (S8's own wrap math) so a narrow phone column never truncates "Resource". The aspect switcher was already built (S1's "Already there" note above); it sits unchanged just above the new chip row.
- [x] Builder "Preconstructed" (reset to this identity's precon) and "Clear" (back to the identity set only).
  - **Landed (`game-client-engineer`, 2026-09-17).** `view/deck-builder-model.ts`'s `resetToPrecon(deck, identity, starterDecks)` (null when the identity has no published precon — Preconstructed then draws unavailable with that reason, "dim, don't hide" rather than being omitted) and `resetToIdentitySet(deck, identity, pool)` (drops every added card back to `requiredIdentitySet`, keeping the deck's own aspects). Both keep the deck's own id/name/source, so neither button hands back "a different deck" — only `cards` (and, for Preconstructed, `aspects`) change. Tested in `view/deck-builder-model.test.ts`, including that Preconstructed's result round-trips to a real, engine-legal Spider-Man precon.
- [ ] Deck advice ("Light on thwart…"), **only after** §4's decision on advice.

- **Fidelity pass (`game-client-engineer`, 2026-09-18).** Compared against P04/D04 in headless Chrome over CDP at 1440×900, 1024×768, 768×1024, 390×844. The builder now has D04's three-column desktop split (aspect/filter/cost-curve rail · card pool · "Your deck" ink rail). Deck check stays P04's single centred column (~640px cap) at every width — deliberately: no wider canvas exists for it. **Remaining:** the "UPGRADE" filter chip truncates to "UPGR…" in the builder's left rail (`view/chip-layout.ts`'s width estimate — the same cross-cutting issue W9 flagged); card-cost badges are coloured by broad type, not the canvas's per-cost hues.

Depends on: nothing hard. W2 is where Deck check sits in the setup flow — see the Deck check note above for exactly how W2 should open it.

Done when: every Core precon and a custom deck show a curve, composition and grouped list, in the builder and on Deck check, at desktop and phone sizes. **Verified 2026-09-17**: `pnpm test`/`pnpm typecheck` green (content 268, engine 507, cards 441, client 580 — up from 554; no engine/`@mc/cards`/`@mc/content` change). Not verified by eye in a browser this session — a `vite` run on port 5183 confirmed both new/changed scenes load and transform with no build error, but no visual pass was done; that's still owed.

### W2. Title menu and the setup flow

Canvases: D01, P01 (Title); D02, P02 (Scenario); D03, P03, T-P02 (Seats); D05, P12 (Table setup).

This reverses the Phase 4 decision to fold Scenario → Heroes → Deck into one screen. That decision was made because precons were the only deck choice; Phase 9 decks have since landed.

Already there: all the setup decisions on `scenes/title.ts`, `view/seats.ts` (seat blocking with the engine's reasons), `view/deck-list-model.ts` (`deckOptionsOf`), `view/seed.ts`, `titleFocusOrder`.

- [x] S2, the setup draft state (see §2).
- [x] **Title as a menu** (D01). Continue card, New game, Decks & Collection, Campaign (drawn locked), Settings (opens W4's overlay), footer with card-pool coverage and build version.
- [x] **Scenario select** (D02) on S8's roster, with a data-only detail panel (§4: no blurbs). The record line (S4's `resultsHistoryOf`) is not wired yet — small follow-up.
- [x] **Take your seats** (D03, P03, T-P02). Seat slots, roster with blocked reasons, hero detail panel (obligation, nemesis), "Use preconstructed for all seats", "Deck check ▸" into W1's Deck check (seat 1's deck; Back returns here, "Start game ▸" continues to Table setup).
- [x] S3, the encounter deck preview (see §2), drawn on Table setup.
- [x] **Table setup** (D05, P12): difficulty from `Scenario.villainStages`, modular picker (recommended preselected, never locked), seating/first player with a seed-deterministic Random, encounter deck preview, "The game you'll get", seed + Reroll, "Deal it out".
- [x] Focus routes for every new screen.
- [x] Retire the setup half of `scenes/title.ts`.
- **Landed (`game-client-engineer`, 2026-09-17; merged into `feature/phase4-screen-gaps`).** Title (`scenes/title.ts`, `view/title-menu-layout.ts`): split ink art panel / red rule / paper menu panel on tablet and desktop, one ink ground on phone, per D01/P01; card-pool coverage from `view/card-pool-coverage.ts`. Scenario select (`scenes/scenario-select.ts`, `view/scenario-select-layout.ts`, `view/scenario-detail.ts`): ink header bar (Back, title, step), paper body, card-styled roster rows, dark stage/detail panel, red "Choose heroes ▸". Take your seats (`scenes/seats.ts`, `view/seats-layout.ts`, `view/seat-slots.ts`): four fixed seat-slot cards, the roster (blocked decks dimmed via `view/seats.ts`), a dark hero-detail panel, `usePreconstructedForAllSeats` (`view/setup-draft.ts`). Table setup (`scenes/table-setup.ts`, `view/table-setup-layout.ts`, `view/modular-sets.ts`, `view/table-setup-preview.ts`, `rollFirstPlayerIndex` in `view/seed.ts`): paper body beside a persistent ink sidebar on tablet/desktop, stacked ink-only on phone. Shared: `scenes/roster-panel.ts`, `view/roster-block-layout.ts`, `view/scaling-text.ts`, `view/setup-metrics.ts`; `view/title-layout.ts` deleted (`rectsOverlap` moved to `view/layout.ts`). Four new focus orders in `view/screen-focus.ts`. Dev-only `?screen=scenario-select|seats|table-setup` in `scenes/boot.ts` jumps past Title for screenshots.
  - **Wired at merge time (main session):** Title's Settings launches W4's overlay; Seats' "Deck check ▸" opens W1's Deck check over seat 1's deck (`goToDeckCheckOrTableSetup`), straight to Table setup when nothing is seated yet; W9's "Play this deck ▸" starts Title with `TitleSceneData`, which builds a one-seat draft (`withSeatOne`) and jumps to Seats with the deck seeded (`SeatsData.seedDecks`) so the seat survives the prune before `deckStorage().list()` resolves.
  - **Fidelity pass** at 1440×900, 1024×768, 768×1024, 390×844 against `ScreensDesktop_00–05`, `ScreensPhone_00/_02`, `ScreensTablet_01`. Three bugs it found, fixed with regression tests: Seats' "HEROES — N seats" label overlapped "USE PRECONSTRUCTED" (missing `LABEL_ROOM`); Table setup's "THE GAME YOU'LL GET" heading overlapped the encounter-deck panel (same); Table setup's selected difficulty/first-player buttons were ink-on-ink on phone (skin now ground-aware). Lesson for the layout tests: they check pairwise overlap of returned rects but never that a `LABEL_ROOM` gap exists or that a control's skin contrasts with its ground. Also: headless Chrome's `--screenshot` floors the viewport at ~500px wide, so phone shots need the DevTools Protocol (`Emulation.setDeviceMetricsOverride`).
  - **Not done:** a card-*grid* roster on wide screens (the canvases draw tall entity cards beside the detail panel; this build uses one virtualized list of card-styled rows at every size — simpler, reuses S8, but doesn't use a desktop's width); a per-villain breakdown for Breakout in the detail panel (first villain only, others named); the record line on Scenario select; modular-set tile labels truncate at narrow tablet widths; `APP_VERSION` is a hardcoded `"0.0.0"`; phone's "Play N" shortcut (redundant with Deal it out).

Depends on: S2, S3, S8. S4 for the record line. W1 for the Deck check step (the flow can link straight to Table setup until W1 lands).

Done when: a 1–4 seat game with a non-recommended modular set and a non-default first player starts from the new flow, and its save replays. Also, with at least 100 decks and 20 scenarios loaded (test fixtures are fine), both rosters must:
- scroll smoothly;
- find a deck or scenario by typing part of its name;
- be fully reachable by keyboard and pad;
- never overlap the controls above or below them, at phone, 800×600 and desktop sizes.

### W3. Setup deal and mulligan

Canvases: D06, P13, L05.

Already there: the engine's `mulligan` step and its pending choice, and the generic sheet in `scenes/choice.ts`.

- [ ] A dedicated setup scene shown before round 1, driven by the engine's setup steps and events (not a scripted sequence): villain and main scheme placed, starting threat, setup cards revealed, obligations shuffled in, opening hands.
- [ ] The opening hand as full cards with per-card mulligan toggles, and "Mulligan N" / "Keep all". It answers the same `PendingChoice` the generic sheet answers today, so the command log is unchanged.
- [ ] Other seats' status (kept, still deciding). Since one human plays every seat, the screen steps through seats in the engine's order. The tablet layout (L05) shows all seats at once.
- [ ] The revealed setup card panel and a setup log, reusing `view/log-lines.ts` wording.
- [ ] "Why this matters" hint (P13), **only after** §4's decision on advice.

Depends on: nothing hard. Reads better after W2.

Done when: a 4-seat game's mulligans all go through this screen, and the saved log is identical to the same choices made through the generic sheet.

### W4. Pause & Rules, and Settings

Canvases: D13, P16, L07.

- [x] The menu button on the board chrome (long table "MENU", phone "≡"), and Escape when no mode or overlay is open.
- [x] Pause overlay: status line (scenario · difficulty · round · phase · seat), Resume, Save & quit (saves are continuous, so this is "back to title"), Concede.
  - **Concede is an engine outcome** (decided 2026-09-17, S5.9 / S5 step 6): `scenes/pause.ts`'s confirm sheet dispatches `{ type: "concede", playerId }`; the store's outcome update hands the Board to Game Over, which words it as conceded, not defeated, and the save records `abandoned`.
- [x] Rules reference filtered to the keywords and statuses on the table, with search (needs S6). Also: villain phase order, and the scenario's card list.
- [ ] "Jump to a moment" into the log (needs S7's cursor model, landed, and its still-open read-only board wiring). Drawn unavailable with its reason in Pause.
- [x] **Settings** screen, reachable from Pause and from Title: reduced motion (exists), large card text, sound (once audio exists, Phase 8). Table options (auto-resolve villain phase, rules hints) wait on §4. Title's entry lands with W2.
- [ ] Pass-and-play handoff, **only after** §4's decision.
- **Landed (`game-client-engineer`, 2026-09-17; report lost to a usage-limit cut-off, reconstructed from commit `54d7516`).** `scenes/board/chrome.ts` gained the MENU/≡ button and `scenes/board.ts` opens Pause on it or on Escape when no mode or overlay owns input. New overlays `scenes/pause.ts`, `scenes/rules.ts`, `scenes/settings.ts` over new pure view models (`view/pause-model.ts`, `view/rules-reference.ts`, `view/scenario-card-list.ts`) and layouts (`view/overlay-layout.ts`, `view/pause-layout.ts`, `view/rules-layout.ts`, `view/settings-layout.ts`), each Vitest-covered including no-overlap at phone/800×600/desktop, with three new focus orders in `view/screen-focus.ts`. Rules reference: `@mc/content`'s glossary filtered live to the keywords/statuses in play, with search, plus a client-side source for "exhausted"/"ready"/"facedown boost card" (S6 left them out), the villain phase order, and the running game's encounter card list by set. Settings: reduced motion, text resolution ("sharper text"), and a new `largeCardText` setting wired into Inspect's rules text. **Not done:** the read-only replay board — the `appSession()` seam spans 5 files/16 call sites, and the unmerged `claude/refactor-board-scene-d7b5ff` branch was found byte-identical to what main already has (`c0134e2`) and offers no seam — so "jump to a moment" is an honestly-unavailable panel; and the design-fidelity pass against D13/P16/L07 (this landed before [design-reference.md](design-reference.md) existed).

- **Fidelity pass (`game-client-engineer`, 2026-09-18).** Compared against D13/P16/L07 and the board canvases at the four sizes, over a real one-seat game (`scenes/boot.ts`'s dev-only `?screen=` jump now also reaches `deck-check`, `deck-builder`, `board`, `pause`, `rules`, `settings`). Four bugs found from the screenshots and fixed, with regression tests: Pause's and Settings' rows were sized for the shortest detail text, so longer text clipped into the row below (rows now size to their own text — `estimateWrappedLines`/`toggleRowHeight` in `view/layout.ts`); Pause's status line ran under its ✕ at phone width; Rules reference's "Filtered to what's on your table" caption sat behind Back; the phone board's "≡" was an illegible 9px glyph. **Remaining:** the chrome bar omits the difficulty label BoardLongTable draws beside "1ST PLAYER". *Tooling note:* headless Chrome here has no GPU, so Phaser's WebGL context fails and every shot is black unless Chrome is launched with `--use-gl=angle --use-angle=swiftshader --enable-unsafe-swiftshader --ignore-gpu-blocklist`.

Depends on: S6 for the glossary, S7 for log jump. Pause, Resume, Save & quit and Settings can land first.

Done when: every screen that shows a board can pause, read a rule for a keyword on the table, change a setting, and return to the same state.

### W5. Targeting

Canvases: D09, L06.

Already there: target-select mode in `scenes/board/controller.ts`, dimming, `view/highlights.ts` (blocked targets with the engine's reasons), and the action bar's prompt line.

- [ ] A targeting panel: title and source ("Photon Blast — deal 5 damage to an enemy"), Cancel · Esc, the legal target list with each target's outcome (S5), and the hovered target's confirm line.
- [ ] "Why not the others?", grouped by the engine's reason.
- [ ] The tablet inspector rail (L06), which shows the source card beside the target list.

Depends on: S5 for outcomes. The panel and "why not" can land first with names only.

### W6. Pending choice: defend

Canvases: D10, P15.

Already there: `scenes/choice.ts`, `PendingChoice.authority`, and `decisionLabel` from `view/villain-walkthrough.ts` (which carries the Peril note).

- [x] Incoming attack summary: attacker, base damage, number of facedown boost cards, and any forced interrupt that changed it.
- [x] Option cards with consequences: who exhausts, damage range (S5), whether an ally survives. "Defense events playable now" is live-queried but always empty today (see the note).
- [x] The stack, with the open window marked ("← here").
- [x] "Waiting on": who decides, and the Peril note.
- [ ] "Auto-defend next time this is the only option" and the phone countdown, **only after** §4's decision on auto-resolve.
- **Landed (`game-client-engineer`, 2026-09-17; merged into `feature/phase4-screen-gaps`).** A dedicated presentation for `declareDefender`, branching inside `scenes/choice.ts` (`#drawDefendChoice`/`#drawDefendOption`) rather than a sibling scene — the subscription, resize handling, card-art loading, Inspect's toggle-back channel and the keyboard/pad route were already there. It answers through the same `#toggle`/`#confirm` → `resolveChoice([optionId])` path, so the command log is unchanged. `view/defend-choice.ts` (`defendChoiceViewOf`, 20 tests) words the engine's `defendPreview`/`stackEntries`/`legalActions`/`decisionLabel` into the incoming-attack summary (attacker, base ATK, exact facedown boost count, any forced-interrupt note read off the `enemyAttack` frame's `vars`), each option's title/exhausts/damage-band range/HP-after/consequences (defeat threshold, Tough, Overkill, Retaliate — each only when a band actually shows it), the stack with "← here", and "waiting on". `view/defend-choice-layout.ts` (42 no-overlap tests at the four sizes): main column + stack/waiting-on/Confirm rail on tablet-landscape/desktop (D10), one stacked column with a full-width Confirm on phone/tablet-portrait (P15). No engine/cards/content change; client 741 → 803.
  - **"Play a defense event" is honestly always empty today.** `legalActions` reports `{kind:"choice"}` for every player whenever any `PendingChoice` is open, `declareDefender` included, so no ordinary command is independently legal while this choice shows. The section is live code (`defenseEventsNoteOf`) that starts reporting once a defense event can be modelled as an option of this same choice — an engine change to ask `game-rules-architect` for. D10's populated "Energy Barrier" example is that future; P15's own mock shows the empty case.
  - **Composition.** No single white sheet: cream cards and an ink rail over the dimmed board, per D10/P15 and `ChoiceOverlay`'s "table stays visible underneath" rule. An option card draws at its own content height, top-aligned in its cell.
  - **Fidelity pass** in the browser: a real Rhino/Spider-Man game driven to a live `declareDefender` via CDP, screenshotted at the four sizes beside `ScreensDesktop_08–09` and `ScreensPhone_02–03`; two issues found and fixed from the shots (an oversized two-option card, a dark-on-dark "waiting on" line). **Remaining:** the option cards still carry a lot of empty height at desktop; no dashed placeholder for a defender that isn't a real option; the header doesn't restate "Villain Phase · Step N of 5" (the chrome bar does); long-press-to-inspect isn't wired on the new option cards.

Depends on: S5 for ranges — landed.

### W7. Villain phase

Canvases: D11, P09, L02.

Already there: the step strip, the phase log, auto-advance, and pausing on choices.

- [x] "Happening now" breakdown for an activation: base + boost − defense = damage (or scheme + boost = threat), with the boost cards shown face up once flipped. Every number comes from the engine's events.
- [x] "Queued this phase": each seat's pending activations, in the engine's order.
- [x] The main scheme threat callout ("11 / 12 threat — one more and the scenario is lost").
- [x] The inline interrupt window (P09, L02): when the player has a legal interrupt, show that card with its play button in the walkthrough, alongside "Let it resolve". It dispatches the same answer the choice overlay would.
- [x] Tablet (L02): keep the team rail legible behind the walkthrough.
- **Landed (`game-client-engineer`, 2026-09-18; merged into `feature/native-packaging`).** "Happening now" reads base/boost/defense/damage off `attackResolved` and SCH/boost/threat off `schemeResolved`; boost cards draw face up from `boostCardFlipped`, with `boostCancelled` worded in `view/log-lines.ts`. "Queued this phase" follows `state.step`'s own order (`view/villain-queue.ts`), marking an activation stun or confuse will cancel via the engine's `statusActive` — the one engine change, a single added export from `packages/engine/src/index.ts`, no logic touched. The threat callout reuses `schemePanel`'s threshold (`view/villain-main-scheme.ts`). The inline interrupt window (`inlineInterruptFor`, `scenes/villain-phase.ts`'s `#drawInterrupt`) dispatches the identical `resolveChoice` the choice sheet would, so the log is unchanged. **Tablet (L02):** a per-seat "TEAM STATUS" panel (`view/villain-team-status.ts`, over the Team tab's own `seatRow`) replaces desktop's phase log on the tablet rail and marks the targeted seat, staying legible behind the interrupt; `view/villain-phase-layout.ts` is now keyed on `FormFactor` rather than a phone boolean. Verified against D11/P09/L02 at 1440×900, 1024×768, 768×1024 and 390×844 in a CDP-driven headless Chrome over a real game (e.g. "RHINO ATTACKS YOU — BASE 2 + BOOST 1 − DEFENSE 0 = DAMAGE 3"). **Remaining:** screenshots used a one-seat game, so the tablet rail shows one row where four seats would fill it (the model is seat-count-agnostic and tested).

Depends on: nothing hard. The breakdown may need event fields the engine doesn't emit yet; check `view/villain-walkthrough.ts` before asking for any.

### W8. Board, Inspect and Game Over follow-ups

Smaller gaps in screens that are otherwise built. Each item stands alone.

- [ ] Board: "Legal now: 3 plays · 2 abilities" and "Why illegal?" under the log (Board - Long Table). `legalActions` and `view/highlights.ts` already have the data; nothing draws the summary.
- [ ] Board: the menu button (lands with W4).
- [ ] Board: Team tab "Table talk" hint (P08), **only after** §4's decision on advice.
- [ ] Board: tablet landscape side rail for Team + Log (L01). PLAN.md Phase 4 routes tablet landscape to the long table; revisit that call rather than overriding it here.
- [ ] Inspect: "This card, this game": where the card has been, from the game log (D08, P14, L06).
- [ ] Game Over: "Watch the replay" (needs S7's still-open read-only board wiring), "Export log", "Share result", "Next: <scenario> ▸", a per-seat damage/thwart table (L08), and phone "Tune deck" / "Read log" (P11).

### W9. Decks & Collection layout

Canvas: D14. This overlaps PLAN.md Phase 9 and should be checked off there as well.

Already there: `scenes/decks.ts`, `view/deck-list-model.ts`, `view/deck-status.ts`, `view/deck-import-model.ts`.

- [x] Two-pane layout on wide screens: deck list beside the selected deck's stats (S1). The live card-pool column stays the builder's.
- [x] The deck list uses S8's search and quick filters (aspect, source, legal/blocked).
- [x] Per-deck record and last played (needs S4, including its deck-id gap).
- [x] "Recently changed" — the minimal version: one `updatedAt` per deck, no revision history (see the note).
- [x] Duplicate, Export (decklist text, the inverse of paste import), and "Play this deck ▸" (opens Title/Seats with this deck in seat 1).
- [ ] Deck note, **only after** §4's decision on advice.
- [ ] Owned-card tracking ("226 of 226 cards owned"), **only after** §4's decision.
- **Landed (`game-client-engineer`, 2026-09-17; merged into `feature/phase4-screen-gaps`).** Two-pane `scenes/decks.ts` on wide screens (`view/decks-layout.ts`): the deck list (search + S8 quick-filter chips for aspect/source/"Legal only", the import/export boxes and "New deck" folded in below it) beside the *selected* deck's stats pane (curve, composition, record, "recently changed", and its actions) on a dark ground, matching D14's grounds/hierarchy — `decks-layout.ts`'s header comment has the composition writeup and what's deliberately not reproduced (D14's live-editing "Card pool" column, which is the builder's job, and owned-card tracking, per §4). Narrow screens get the same two groups behind a "Decks"/"Stats" tab strip. A row's tap now **selects** it; every action (Check, Duplicate, Export, Edit/Delete, Play) moved to the stats pane.
  - **Search and quick filters (S8)** reuse `view/roster-filter.ts`'s `heroRosterMatches` unchanged; `deckSourcesOf` (new) derives the source chips from the pool.
  - **Per-deck record and last played (S4)** come from a new `EngineHost#listSaves()`/`SessionStore#listSaves()` (mirrors `latestSave()`) feeding `resultsHistoryOf`, keyed by S4's `DeckKey`. Shown in the stats pane only.
  - **"Recently changed": the minimal honest version.** `Deck.updatedAt?: string` (`@mc/content`), stamped only where a deck is persisted — `touch`/`duplicateDeck` (`view/deck-builder-model.ts`) and both import functions. Old stored decks without it still load (`view/deck-recency.test.ts`); saved decks sort by it (`sortByRecency`); the pane says "No change history recorded for this deck." **No revision history** — one timestamp, overwritten on save.
  - **Duplicate, Export, Play this deck ▸.** `duplicateDeck` always yields an editable `userBuilt` copy. `exportDecklistText` (`view/deck-import-model.ts`) is the exact inverse of `parseDecklistText`, round-trip tested against every Core precon; Export copies to the clipboard with an honest failure message when unavailable. "Play this deck ▸" is live: `#playDeck` starts Title with an optional `TitleSceneData` (`initialSeatDeckId`/`initialSeatDeck`) and `view/setup-draft.ts`'s new `withSeatOne` seats exactly that deck; disabled with the engine's reason when the deck isn't legal. *W2's Title rewrite must keep this entry point* (or route it to the new Seats screen).
  - **Shared stats widget** factored out rather than copied a third time: `ui/deck-stats-widgets.ts`'s `drawCostCurveBars`/`drawCompositionTiles`/`drawGroupedCardList`, now used by `scenes/deck-check.ts`, `scenes/deck-builder.ts` and this screen.
  - **Fidelity pass** at 1440×900, 1024×768, 768×1024 and 390×844 against `ScreensDesktop_12`/`_13` (headless Chrome over CDP). One bug found and fixed by it: switching to the narrow "Stats" tab left the DOM-backed search/import fields floating over it. **Remaining differences:** D14's three columns are two here (the middle "Card pool" grid is D04's); the owned-card coverage line is skipped (§4); at 390px `Aggression`/`Leadership`/`Protection` chips truncate — `view/chip-layout.ts`'s width estimate is at its margin for 10-character labels and is shared with Title, so it's left for a chip-layout fix.
  - **Skipped, per §4:** deck note and owned-card tracking.
  - Tests: `view/decks-layout.test.ts`, `view/deck-recency.test.ts`, `view/deck-builder-model.test.ts`, `view/deck-import-model.test.ts`, `view/roster-filter.test.ts`, `view/screen-focus.test.ts`. Client 580 → 617.

Depends on: S1, S4.

## 4. Decisions to settle

- **Advice text.** Deck notes (D14, P04), "Why this matters" (P13) and "Table talk" (P08) are strategy advice, not rules. If kept, each needs a named heuristic in a tested view model, worded as a suggestion, and never using hidden information. Or drop them.
- **Scenario and hero blurbs** (D02, D03, P03). They aren't in `@mc/content`. Either write short original descriptions, or show only data (stages, HP, sets, obligation, nemesis). Don't copy FFG card or product text into them.
- **Keyword glossary wording** (S6). Short paraphrases with an RRG 1.8 page cite, rather than transcribing the RRG.
- **Auto-resolve and auto-defend** (P12, D10, P15). A client preference that answers a choice for the player is still the player's command. It must issue ordinary commands and never change engine behavior. Decide whether it's in scope before Phase 5 makes seats remote.
- **Heroic difficulty** (D05). Not a Core Set mode and not in the engine. Leave it out, or draw it unavailable.
- **Concede** (W4): DECIDED 2026-09-17 — an engine outcome (`concede` command, `conceded` result), per S5.9.
- **Pass-and-play handoff** (P16, L07). Only meaningful with several humans on one device, which means hiding hands between seats. Decide whether that's Phase 4 or Phase 5.
- **Owned-card collection** (D14). With only Core implemented every card is "owned". Probably out of scope until Phase 7 adds packs someone might not own.
- **Seed display** (D05 shows `4F2C-91A7`). Today seeds are whole numbers typed and shown as digits (`view/seed.ts`). A hex display needs a lossless round trip with what the player types back in.

## 5. Mock content that is wrong for the Core Set

Build these from `@mc/content`, not from the canvases:

- **Heroes.** Ms. Marvel, Thor, Black Widow and Captain America are not Core heroes. The D03 roster says "5 identities" but omits Iron Man, who is one.
- **Difficulty.** P02's "STD I / STD II / EXPERT" is not a Core mode. Core has Standard and Expert (`Scenario.villainStages`).
- **Modular sets.**
  - D05 lists "Ultron Drones" as a modular set. The Core modulars are Bomb Scare, Masters of Evil, Under Attack, Legions of Hydra and The Doomsday Chair.
  - D05 and P12 mark Masters of Evil "required by Klaw · locked". The data models each Core scenario's modular as *recommended* (`recommendedModularSetIds`: Rhino → Bomb Scare, Klaw → Masters of Evil, Ultron → Under Attack). Confirm against the RRG before locking any modular in the picker.
- **Stats and text already flagged in PLAN.md Phase 4:** Sonic Boom's text, Klaw's "SCH 14", Defense Network's "+1 DEF".

## 6. Suggested order

1. **W1**, starting with S1. Small, unblocks the analysis in two places, and needs no engine work.
2. **W2**, with S2, S3 and S8. S8 can land first, on today's Title seats and scenario row, since the pool has already outgrown them. The largest visible gap, and it gives W1's Deck check its place in the flow.
3. **W3**. Self-contained once W2's flow exists.
4. **W4**, starting with Pause, Resume, Save & quit and Settings. The glossary follows S6, and log jump follows S7.
5. **S5**, then **W5** and **W6**. Blocked on engine queries, so start the `game-rules-architect` design early and build the parts that don't need it.
6. **W7**, **W8** and **W9** as independent follow-ups.
